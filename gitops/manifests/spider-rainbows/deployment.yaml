apiVersion: apps/v1
kind: Deployment
metadata:
  name: spider-rainbows
  namespace: default
  labels:
    app: spider-rainbows
  annotations:
    test-webhook-sync: "2025-11-05T01:10:00Z"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: spider-rainbows
  template:
    metadata:
      labels:
        app: spider-rainbows
    spec:
      containers:
      - name: spider-rainbows
        image: wiggitywhitney/spider-rainbows:v1-main-7044cc0
        ports:
        - containerPort: 8080
          name: http
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
          allowPrivilegeEscalation: false
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
